---
name: calmcarry-project
description: "CalmCarry companion app — Glowco/Mason $10k build; \"eucalyptus calm\" hybrid light/night direction locked; Expo SDK56 + NativeWind scaffolded, Tonight+Wind-down screens verified on web"
metadata: 
  node_type: memory
  type: project
  originSessionId: fed93e3e-af57-4134-8f31-04774275f858
---

CalmCarry = companion mobile app for **Glowco's physical calm/sleep device**. Client contact: **Mason** (Glowco International L.L.C FZ). Built by Task Force AI Development Services L.L.C.

**Deal:** closed at **$10,000 USD** fixed (first-engagement goodwill rate; Mason opened at $5k). Milestones: $3k deposit + $2.5k (design approved) + $2.5k (feature-complete build) + $2k (live on stores). Invoice **TF-INV-2026-001** at `Downloads/CalmCarry_Invoice_TaskForce.pdf` (light theme, Wio Bank IBAN AE43 0860 0000 0901 2001 576 / SWIFT WIOBAEADXXX + USDT TRC20 fallback; no VAT — not registered). Quote at `Downloads/CalmCarry_Quote_TaskForce.pdf`.

**Stack (decided):** Expo (React Native) + **NativeWind** (Tailwind-for-RN) + **Moti/Reanimated** for motion. Backend NestJS + Postgres + Shopify **bundle-entitlement** unlock (NO in-app purchase). Scope contract: `~/calmcarry/V1_SCOPE.md`. Glowco provides all audio/assets/accounts.

**Design — Mason's #1 priority.** Reference he sent: **bettersleep.com** = a VIBE ref (dark immersive premium sleep app), NOT the brand. **CORRECTION 2026-06-20:** the earlier "candlelit nightfall" amber-on-indigo palette was WRONG — it was built off the competitor and never checked the client's real brand. **Glowco = The Glow Company (theglowcompany.co), a natural sleep/wellness brand** (sleep gummies etc.). Real brand palette to match: deep teal `#426768` (primary), sage/eucalyptus accent `#74a5a2` (btn hover `#466d6a`), mint bg `#d3edea`/`#c5e7e3`/`#b3e0da`, cream `#f2f1eb`, slate-green text `#485453`, white, sparing coral pop `#ef626c`. **DECIDED 2026-06-20: HYBRID** — light/airy on-brand app (mint/cream/sage, slate text) for day/utility screens + brand-derived dark NIGHT MODE (deep eucalyptus `#0E1817`/`#15231F` base, sage glow `#8FC9BE`) for wind-down/player/sleep screens. `~/calmcarry/DESIGN_SYSTEM.md` fully rewritten to the brand (light + night token sets, §7 anti-AI-slop charter, §8 architecture). Mockup re-skinned to 2 light + 1 night screen at `Downloads/CalmCarry_Mockup.png`. Type: **Montserrat** (their exact brand font, verified live from theglowcompany.co — site body/headings/buttons all Montserrat) for all UI + most headings; **Fraunces** serif accent for hero/display (free stand-in for their licensed "Silk Serif"). Logo = "CalmCarry" wordmark in Fraunces + the sage GlowOrb as the app-icon mark on a mint ground. Superseded amber direction below: off-white `#F4F1FA` titles, lavender `#9C93C4` body; serif **Fraunces** (hero) + sans **Sora** (UI); slow breathing-orb / parallax / wind-down-dim motion. **Differentiators vs BetterSleep:** physical-object companion (Device tab, breathing paired-orb, authenticity check, warranty) + no paywall (bundle unlocks) + calmer (mixer capped 4-6 tiles, one action/screen). Full direction generated via workflow; 3-screen mockup at `Downloads/CalmCarry_Mockup.png` (source `/tmp/calmcarry_mockup.html`). **Design system LOCKED at `~/calmcarry/DESIGN_SYSTEM.md`** — cross-validated by the user's design tools: **ui-ux-pro-max** (installed `~/.claude/skills/ui-ux-pro-max-skill`; query `python3 src/ui-ux-pro-max/scripts/search.py "<q>" --domain color|style|typography|ux | --stack react-native`) + **emil-design-eng** skill. Motion tokens: `Easing.bezier(0.16,1,0.3,1)` enter/exit, `withSpring damping:20 stiffness:90`, scale-0.97 press, transform/opacity only on UI thread, breathing orb withRepeat 4000ms. RN recipe = "Cinema Mobile" (LinearGradient base + Reanimated blurred ambient blobs + expo-blur nav + haptics).

**BUILT 2026-06-22 (milestone 2 scaffold):** Real standalone git repo at `~/calmcarry` (its own `.git`, isolated from the accidental `~/` home repo). Expo **SDK 56** (RN 0.85, React 19, React Compiler on, New Arch) via `create-expo-app` default template (expo-router, `src/` layout). NativeWind **4.2.5** + **tailwindcss v3** (NOT v4 — NativeWind v4 needs Tailwind 3), Reanimated 4 + react-native-worklets, Moti, expo-blur/haptics/linear-gradient, @expo-google-fonts montserrat+fraunces, @expo/vector-icons (Feather = the one outline family). babel: `babel-preset-expo {jsxImportSource:'nativewind'}` + `nativewind/babel` (worklets+react-compiler auto-included by preset — do NOT add worklets plugin manually). Tokens live in `src/theme/` (colors/typography/motion/ThemeContext); theme is **per-screen** via `<Screen mode="light|night">`, NOT OS color scheme. Primitives in `src/components/`: AppText, Screen, GlowOrb (breathing), PrimaryButton (scale-0.97+haptic), CoverCard, StatusChip, TabBar (BlurView, 4 tabs). Screens: **Tonight** (light, `(tabs)/index`) + **Wind-down** (night, `app/wind-down.tsx`, wired to the Begin-wind-down button) — both verified on web + screenshotted, both themes proven. `tsc --noEmit` clean. **No native simulator on this Mac** (Xcode CLT only, no Android SDK) → verify on Expo web (`npx expo start --web`, port 8081 via `.claude/launch.json`); Expo-Go-ready for device. **NOT yet committed** (awaiting user go-ahead). README written. **EXPANDED 2026-06-22 (motion + media pass):** (1) Pervasive CALM motion added per a research+audit workflow (github topics ui-ux/website-design are ~all web-only — harvested ideas not code) then an adversarial review workflow (10 confirmed fixes applied): wind-down dim-arc scrim + SVG timer ring (depletes), controls auto-fade, play/pause glyph crossfade, modal route transitions; TabBar color crossfade + haptic; CoverCard ripple breathing; Screen light-mode mint drift; shared `Reveal` stagger. (2) **Higgsfield** cover art: the connected MCP is server **eb8a5a8a-…** (Higgsfield — Soul/Recraft/Marketing Studio); workspace plan=ultra. Used **Recraft 4.1** (`model_type:'standard'`) with its **`colors` param** set to the brand hexes to lock a cohesive illustrated series — ~1.25 credits/image. 8 covers in `assets/covers/*.webp` (~830KB), registry `src/content/covers.ts`; rendered via **expo-image** in CoverCard + new `LibraryCard`. Built `src/features/sounds/SoundsLibrary` (BetterSleep-style rails, lock badges for paid). (3) **Sleep tracking UI** (`src/features/sleep/`): night-mode `SleepSummary` (Segmented Summary/Sleep-phases/Recordings + ScoreGauge 270° SVG arc + 2×2 stat tiles + `SleepPhasesChart` SVG hypnogram + legend), reached via a "Last night" card on Tonight → `/sleep-summary` modal route. Mock data — real sensor-based sleep detection (mic/accelerometer) is **Phase 2 ($$), per V1_SCOPE** (advanced tracking is explicitly out of the $10k). Covers are AI CONCEPT art; Glowco supplies licensed finals. **NON-OBVIOUS RN/web LESSONS (this stack):** on react-native-web, Reanimated layout `entering` anims (FadeInDown) get STUCK partway when children load images async (layout shift) → drive entrances with a self-contained shared-value `useEffect`+`withTiming` instead (see `src/components/Reveal.tsx`); **expo-image `transition` stalls at opacity 0 on web** for cached/fast images → omit it; `react-native-svg` + `useAnimatedProps(strokeDashoffset)` works on web (rings/gauge/draw-on); import nav only from `expo-router` (it forked React Navigation in SDK 56). Still NOT committed (awaiting go-ahead).

**MASON'S AUTHORITATIVE SPEC** (found 2026-06-22): `~/Downloads/CalmCarry_Companion_App_Build_Plan_v2.docx` (v1 + `CalmCarry_Prototype.html` also there). v2 = **PAID, content-led, bundle-tier**: monetize via a higher-priced device bundle ("Calm Plan", ~$79) granting LIFETIME premium — NOT a subscription. **§10 entitlement-not-IAP: the app must NEVER show a price/buy button for premium** — check Shopify bundle order on login, surface owned content, point to theglowcompany.co to buy (dodges the 30% cut). Device has NO Bluetooth → "hold CalmCarry while this plays" (app can't control it). Content production is the real critical path. Compliance: wellness-not-medical, swap-table words, disclaimer required.

**BUILT 2026-06-22 (full v2 feature frontend):** `src/content/library.ts` (TRACKS/PROGRAMS/LEARN + WELLNESS_DISCLAIMER). New routes: **Player** (`/player`, night, cover-in-progress-ring + "hold your Glow Orb" + download toggle), **Programs** (Sounds rail + `/program` step list), **Learn** (`/learn` + `/learn-article` w/ §10 disclaimer), **Calm Plan paywall** (`/unlock`, §10-compliant: NO in-app price, web CTA), **Family/kids** (`/family` stub). LibraryCard/CoverCard → player or /unlock when locked. New primitives **Segmented** + **FormField**. **16 review fixes applied** incl. WCAG AA contrast: light.muted→#5C6968, light.dim→#6B7676, night muted/dim opaque, NEW `textAccent` token (=teal) so text-accent (AppText tone="accent", SectionHeader, PrimaryButton secondary) is readable while `accent` (sage) stays for icons/fills; motion tokens `dur.draw`/`dur.reveal`. tsc clean; verified on web. **Remaining = the paid backend:** NestJS+Postgres+Shopify entitlement/order-webhooks + headless CMS + signed-URL audio CDN; real audio (expo-audio); "were you settled?" check-in; Glowco's content production. **DARK MODE added 2026-06-22:** app-level appearance pref (Light/Dark/System, default System→follows OS) via `src/theme/ColorScheme.tsx` (ColorSchemeProvider at root, `useColorSchemePref().effective`). `Screen` resolves mode: `mode==='night'` forces dark (sleep screens), otherwise = effective (adaptive utility screens flip to the night theme in dark mode). Toggle = Segmented in Account → Appearance. Required migrating ALL hardcoded `brand.white`/`light.*`/`brand.mint(Soft)` → theme tokens (`c.surface/line/shadow/lineSage/panel/panelStrong/textAccent/accent`) across every adaptive screen + TabBar (chrome reads useColorSchemePref directly since it's outside Screen); added `panel`/`panelStrong` tokens. Verified: System (OS dark), manual Dark, manual Light-override all work; sleep screens unaffected. Pref is IN-MEMORY (persist w/ AsyncStorage later). Rule going forward: NEVER hardcode brand.white/light.* for adaptive UI — use `c.*` from useTheme so dark mode stays correct.

**FRONTEND ROUND 2026-06-22 (frontend-only, no backend yet):** Added **AsyncStorage** (`@react-native-async-storage/async-storage` 2.2.0) → ColorScheme pref + onboarding flag now PERSIST (web=localStorage). **Onboarding** (`/onboarding` 4-slide welcome) gated on first run via root `_layout` using `useRootNavigationState` + `getOnboarded()` (`src/lib/onboarding.ts`, key `cc.onboarded`) → redirects, "Get started"→`markOnboarded`+`/auth`. **"Were you settled?" check-in** (`/check-in`, night) wired to wind-down close (router.replace). **Library search** (`/search`, FormField filter over TRACKS+PROGRAMS → CoverCard results; search icon added to Sounds header). **App icon**: generated CalmCarry GlowOrb (sage→deep-eucalyptus orb on mint) via Higgsfield/Recraft → `assets/images/icon.png`/`splash-icon.png`/`favicon.png`/android-icon-foreground; `app.json` updated (name "CalmCarry", icon, adaptiveIcon bg #D3EDEA, splash bg #D3EDEA, removed expo.icon/blue defaults). All verified on web; tsc clean; 21 routes. NOTE: expo-router typedRoutes is ON — dynamic `router.push(stringVar)` needs `as Href` cast.

**MOTION-GRAPHICS + REVIEW ROUND 2026-06-22:** Added calm "alive" motion: GlowOrb **aura** (2 staggered sonar ripples, `aura` prop, on feature orbs), Screen **DriftBlob opacity-breathe + floating Motes** (night), **hypnogram draw-on** (SVG strokeDashoffset). Ran adversarial review (13/17 confirmed) and fixed all: **(KEY) Reveal was SSR-blanking** — it started opacity:0 and `useReducedMotion` returns false during static render, so the web `output:static` HTML shipped `opacity:0` on all content until hydration. Fixed by `ANIMATE_ENTRANCE = Platform.OS !== 'web'` → entrances animate on NATIVE (the product), web renders statically visible. Verified 0 `opacity:0` blocks in SSR HTML. Also: onboarding first-run gate restructured into an inner `RootNav` inside ColorSchemeProvider, gated on native only (`Platform.OS==='web' || (onboarded!==null && schemeHydrated)`) with a redirect-once ref — no first-run flash on native, no SSR blank on web; ColorScheme now exposes `hydrated`. `freezeOnBlur:true` on Tabs (stops off-screen Reanimated loops — perf, since a night screen runs ~13 loops). FormField a11y label falls back to placeholder; Skip tap targets→44px; aura/draw/reveal durations + mote/gauge colors tokenized. tsc clean. **LESSON: web is PREVIEW only (native is the product) — animate-on-native/static-on-web for SSR-safety; never start content at opacity:0 without a Platform/SSR guard.**

**FULL MVP (FE+BE) built 2026-06-22 (autonomous, AFK):** Now a working full-stack MVP. **Backend** `~/calmcarry/server` = NestJS + SQLite (TypeORM/better-sqlite3@12, JWT, bcrypt), modules auth/users/content/devices/logs, auto-seeds on boot, port **4000**, CORS open. Demo user **sarah@theglowcompany.co / glow1234** (active calm_plan + seeded device). `cd server && npm run start`. **Real audio**: 5 bundled royalty-free files in `assets/audio/` (ocean/rain/forest = CC0, drone.ogg = public-domain, piano.mp3 = CC BY → credited in Account via `AUDIO_CREDITS`); `src/content/audio.ts` registry; tracks gained an `audio` key; **expo-audio** wired in Player + wind-down (loop/play/pause; metro assetExts += mp3/ogg/m4a/wav). **App data layer**: `src/lib/api.ts` (fetch client, 3.5s timeout, throws→fallback), `src/lib/store.ts` (AsyncStorage), `src/lib/sessions.ts` (logSession local+API), `src/features/auth/AuthProvider.tsx` (API-first sign-in with **local fallback so the app always works offline**; isPremium from entitlement). SignIn prefilled w/ demo creds → real JWT. Account shows real user+entitlement+signout. Entitlement gates library/programs/search (premium unlocks). RegisterDevice + check-in POST to API best-effort. **VERIFIED on web**: real JWT login (261-char), /me calm_plan, library unlocked, player audio object live+controllable, all 5 endpoints. Both servers left RUNNING (API 4000 + Expo 8081). `RUN_MVP.md` + `start-all.sh` at repo root document how to run (Expo Go on phone = best audio; web = tap to play due to autoplay policy). tsconfig excludes `server/` from app tsc. **STILL NOT COMMITTED** (per instruction). Backend caveats: synchronize:true (use migrations before deploy); Shopify entitlement stubbed locally (real = Shopify Customer-Account-API + order webhooks, Glowco's keys).

**WHAT'S LEFT to ship v1 (per build plan):** (a) **Backend** (the paid Phase 1-2): NestJS+Postgres, Shopify Customer-Account-API login + order-webhook→entitlement service, headless CMS, signed-URL audio CDN. (b) **Real audio** (expo-audio) + background play + actual offline downloads. (c) Onboarding/post-purchase flow; "were you settled?" check-in→log/insights; push reminders (APNs/FCM); library search; persisted prefs. (d) Release: app icon+splash (currently Expo defaults), EAS build, App Store/Play submission + bundle SKU live, compliance pre-submission checklist, tests, analytics. (e) Glowco's CONTENT production (scripts/narration/audio — the real critical path). (f) Repo still NOT committed. Most vibe-coded design tools Mason knows (21st.dev, Aceternity) are WEB-only — don't use them in the RN app. See [[fluido-project]] for the user's other live RN/React build.

**ROUND 2026-06-22 (modes + survey + real logos + device purchase):** (1) **Kids/Adult mode** + **12h personalization survey** added: `src/features/profile/ProfileProvider.tsx` (mode/mood, AsyncStorage `cc.mode`/`cc.mood`/`cc.lastOpen`, `SURVEY_GAP_MS=12h`), `/survey` (`MoodSurvey`, §10 swap-table copy NOT clinical), mode-aware Tonight/Sounds, toggle in Account + Family. (2) **REAL LOGOS** now in `assets/brand/` — downloaded from the user's Drive folder (sage `calm CARRY™` wordmark + a white version); trimmed via PIL to `wordmark-sage.png`/`wordmark-white.png` (1000×566). `src/components/Logo.tsx` rewritten = theme-aware Image (sage on light / white on night), replaces the old synthetic orb+Fraunces lockup; used on Tonight/SignIn/Onboarding. Drive owner domain seen = **glowdermal.com.au** ("The Glow Company"). (3) **Buy/replace device IN-APP** = new `src/features/device/DeviceShop.tsx` (`/shop`) + `src/content/store.ts` (`openCheckout` → `Linking.openURL`); entry points in DeviceHub ("Get more → Buy another Glow Orb") + Family. **Physical goods MUST use external web checkout (Apple 3.1.5a / Play forbid IAP for hardware)** — price/shipping deferred to checkout, no in-app price. `DEVICE_CHECKOUT_URL` = `theglowcompany.co/products/calmcarry-glow-orb` is a **PLACEHOLDER — point at the real product listing before ship**. (4) **KEY THEME FIX:** added a **root `<ThemeProvider mode={effective}>`** in `_layout.tsx` — without it, a screen body's `useTheme()` (runs ABOVE its own `<Screen>`) fell back to the DEFAULT LIGHT context → white card surfaces under near-white night text (invisible) in dark mode. Also: surface styling that lives in a screen BODY must be moved into a child component (which re-reads useTheme inside the Screen) — done for survey/DeviceHub summary/check-in option cards. (5) **WCAG fixes from an adversarial review workflow:** `light.ctaBg` changed **sage `#74A5A2` → deep teal `#426768`** (white label was 2.75:1, now ~6.2:1) — this is a DELIBERATE deviation from "sage buttons"; do NOT revert without re-checking AA. Info-bearing check icons use `c.textAccent` (teal) not `c.accent` (sage). (6) **CalmPlan compliance:** removed the external "Get the Calm Plan bundle" web-buy button (Apple 3.1.1/3.1.3 polices steering for DIGITAL goods) → now **"Sign in to unlock"** → `/auth`, store reference is plain non-tappable text. All verified on web (light+dark), tsc clean, re-exported to `dist/` served on :3000. STILL NOT COMMITTED.

**⚠️ DEFINITIVE PLAN 2026-06-23 — supersedes the old v2 .docx. `~/Downloads/CalmCarry_App_Build_Plan.pdf` (v1.0, "for Nido") is now the authoritative spec.** It DIFFERS from what was built; user said "do the same and better, audit harshly, fix everything." Did a harsh audit + fixed all hard violations (verified on web, tsc clean, re-exported):
- **NO "paired"/"connected" EVER** (device has no Bluetooth). Purged from Tonight/Family/Learn; cues are now sensation-honest ("rest it in your palm / a gentle pulsing", never buzz/vibration). HARD RULE.
- **Sleep tracking CUT** (plan decision — clinical feel). DELETED SleepSummary/SleepPhasesChart/ScoreGauge + the "Last night · 82" card + /sleep-summary route. Do NOT rebuild it.
- **MONETIZATION FLIPPED**: plan locks **freemium + in-app SUBSCRIPTION via Apple/Google IAP, $12.99/mo or $69.99/yr (annual-leading), no free trial**, gentle in-context upsell, one-tap cancel + pre-charge reminder. This OVERRIDES the old "§10 no in-app price / entitlement-not-IAP / point-to-web" rule (that was the OLD docx). Rebuilt CalmPlan (/unlock) to show prices + plan toggle + restore + honest-billing copy; `PRICING` in `src/content/store.ts`; `activatePremium()` added to AuthProvider (DEMO stand-in for IAP receipt validation). The PHYSICAL device still sells via external web checkout (correct — Apple forbids IAP for physical goods); only the DIGITAL subscription uses IAP.
- **Check-in is now FORWARD-looking** ("What would feel good right now?": sleep/reset/sounds/suggest) — ProfileProvider `mood`→`intent`. NO symptom tracking / mood history (removed the retrospective "how did you settle?" rating + logging; CheckIn is now a gentle no-rating close).
- **Parent gate** (non-negotiable child safety): `/parent-gate` PIN screen (`src/lib/parentGate.ts` + `src/features/parentgate/`), required to LEAVE kids mode (entering is open). Wired into Family switch + Account mode toggle. Verified create+enter+wrong-PIN-reject.
- **BUILT 2026-06-23 (structural pass, all verified on web, tsc clean, re-exported):** **5-tab IA** now live — Home / Library / Listen / Community / Profile (`(tabs)/_layout` + TabBar TABS map; index=Home, sounds=Library, NEW listen, NEW community, you=Profile). **Device moved OUT of tabs** → pushed screen `src/app/device.tsx` (registered in `_layout`, DeviceHub got a back button), reached via Profile → "My CalmCarry". **Sound machine** = `src/features/listen/ListenScreen.tsx` (4 looping expo-audio players rain/ocean/forest/drone, tap-to-layer, per-sound 3-level volume, fade-out sleep timer Off/15/30/60, save/load mixes to `cc.mixes`, Stop all) — verified tile toggles + playback state. **HERO Player upgraded** = breathing halo (4s in / 6s out, reduced-motion-gated) + "BREATHE IN/OUT" pacer + rotating sensation-honest CUES array. **Community** = `src/features/community/CommunityScreen.tsx` (presence line, anonymous "a CalmCarry parent" wins wall, gentle composer, Anonymous + Adults-only chips, gently-moderated note). **KIDS MODE made genuinely kid-like** (verified): `src/components/BearMascot.tsx` = "Bramble", a sleepy SVG bear (sage head, cream snout, rosy cheeks, closed eyes, gentle breathing — a nod to the kids "mini bear" device); `src/features/kids/KidsHome.tsx` = big picture-led home (Fraunces "Hi {name}!", Bramble, non-failable "calm nights" stars from `cc.calmNights`, big penguin story hero, big calm-sound tiles) — TonightScreen early-returns `<KidsHome/>` when `mode==='kids'` and skips the adult check-in. TabBar hides Community + Profile in kids mode (KID_TABS = index/sounds/listen); a discreet **parent-lock** on KidsHome routes to the parent gate to exit. Entering kids (Account/Family toggle) now `router.replace('/')` so no orphaned hidden tab.
- **TYPOGRAPHY migrated to plan §16 (2026-06-23, verified):** body/UI = **Poppins** (`@expo-google-fonts/poppins` installed), headings/display = **Montserrat**. Fraunces DROPPED (removed from fontMap + all literals repointed to Montserrat_700Bold in KidsHome/ProgramDetail). `src/theme/typography.ts` rewritten (fonts.regular/medium/semibold/bold=Poppins, fonts.heading/display=Montserrat). Kids-mode Community/Profile tabs hidden via TabBar (already noted). **Teal-pill active tab** added to TabBar (opacity-fade pill, `rgba(143,201,190,0.22)` night / panelStrong light — no sliding, §4-safe).
- **FINISHED 2026-06-23 ("finish the whole product tonight"), all verified on web + tsc clean:** **Multi-profile household** — ProfileProvider now holds `profiles[]` + `activeProfile`; `mode` is DERIVED from the active profile's type (setMode kept = switch to first profile of that type). `ProfileSwitcher` on adult Home + Family; `addProfile` (Family). Persists `cc.profiles`/`cc.activeProfile`; migrates legacy `cc.mode` once. **Home shelves**: dismissible "How CalmCarry works" intro (`cc.hiwDismissed`) + "New this month" rail. **Free-sampler split** (library.ts `locked` flags): free = slow-tide/box-breathing/deep-rest/penguin/spa(music); locked = rainfall/forest/letting-go + all programs. **Watch & learn** rail in Library → /learn-article (adults; CMS video later). Renamed the "melatonin off-ramp" program → "Evening Wind-down" + removed the melatonin community seed (wellness-not-medical).
- **CHILD-SAFETY HARDENED after a 4-lens adversarial review (12 high found, fixed):** **systemic `KidsGuard` in `_layout.tsx`** — when `mode==='kids'`, any route NOT in `KID_ALLOWED=['/', '/sounds', '/listen', '/player', '/parent-gate']` is `router.replace('/')` (blocks /unlock, /you, /search, /family, /shop, etc. via in-app push OR deep link — tab filtering alone was insufficient). Parent gate: `close()` falls back to '/' (never /you); intents `enterKids`/`exitKids`/`setup` — **entering kids requires a PIN to exist first** (`hasParentPin()` in Account/Family `enterKids()` → routes to `/parent-gate?intent=enterKids` to create one in the adult context, closing the create-and-exit hole). SoundsLibrary in kids: hides Search/Learn, never routes to /unlock, no lock badges. **a11y fixes:** ProfileSwitcher active avatar uses teal (was white-on-sage 2.75:1), KidsHome/ListenScreen tile scrims raised to ~0.55 for label contrast, ListenScreen volume pegs got role/state + 44px target, KidsHome stars + BearMascot a11y labels. **Verified live:** /unlock & /you redirect home in kids mode; profile switch You↔Leo works. Re-exported to :3000. STILL NOT COMMITTED. (LAN IP is now 192.168.1.136.) Buttons still 16px radius (plan says 8 — deliberate). Companion docs (Who The App Is For, Competitor Teardown, Kids Sleep Apps Teardown, Why We Win) in the plan folder.

**BACKEND BUILT OUT 2026-06-23 (production-shaped; all secrets are placeholders + dev-fallback so it runs with zero keys):** `~/calmcarry/server` (NestJS) expanded to the full plan §8/§9 surface — built + booted + curl-verified (26 routes, no errors). New entities: Profile, SavedMix, CommunityPost, PushToken; Entitlement extended (source/plan/productId/transactionRef/expiresAt). New modules: **Billing** (`POST /billing/validate` IAP receipt→entitlement, `GET /billing/status`), **Profiles** (household CRUD), **Community** (anon wins wall + light moderation), **Notifications** (push-token register/test), **Ownership** (`POST /ownership/match` Shopify-by-email + `/webhooks/shopify/orders` HMAC), plus `POST /auth/social` (Apple/Google), `GET /content/:id/signed-url` (CDN), `POST /checkin/recommend` (ephemeral), `POST /admin/content` (CMS publish, x-cms-key header). **Integrations layer** `server/src/integrations/`: ReceiptValidation (real Apple verifyReceipt behind keys; Google guarded), Shopify, Cdn (HMAC signed URLs), Push, SocialAuth — each DEV-FALLBACKS when its key is absent. Placeholder hub = `server/src/config.ts` + **`server/.env.example`** (every key); startup logs which integrations are live. Docs = **`server/BACKEND.md`**. Seed now also creates the household (Sarah adult + Leo kid) + 4 community posts; content free-split + program rename (evening-ritual) mirrored. Before prod: `DATABASE_URL` Postgres + turn off `synchronize` (migrations), real keys, JWKS verify for social, Google Play subscription lookup. **APP NOT YET WIRED to the new endpoints** (billing/profiles/community) — app still uses local state + local fallback; point `src/lib/api.ts` at the deployed URL + call them when ready. Backend left RUNNING on :4000 (node dist/main.js, fresh re-seeded DB). STILL NOT COMMITTED.

**AUTONOMOUS "while-you-sleep" pass 2026-06-23 (no keys, no input):** (1) **App↔backend wired** for Community (`api.communityPosts`/`createPost` — CommunityScreen fetches live presence+posts, falls back to local seed) and Billing (CalmPlan `subscribe()` → `api.billingValidate` then local `activatePremium`; offline-safe). Added all new endpoints to `src/lib/api.ts` (billing/profiles/community/recommend/ownership/registerPush/signedUrl). Profiles sync NOT wired (app still local; backend ready). tsc clean, re-exported. (2) **Launch pack** generated via workflow (no keys) → `docs/STORE_LISTING.md` (App Store + Play copy, "not a medical device" in first para, compliance-checked), `docs/PRIVACY_AND_DATA_SAFETY.md` (Apple App Privacy + Google Data Safety answers), `docs/SESSION_SCRIPTS.md` (6 ready-to-record narration scripts, sensation-honest), `docs/WHY_WE_WIN.md` (positioning + parity table). (3) **`HANDOFF.md`** at repo root = master index (run, what works now, exactly what Mason must provide + keys, go-live steps). Compliance-grepped the docs clean. App :3000 + API :4000 both running.

**DE-FAKE PASS 2026-06-23 ("dont fake any of the features/functions"):** Swept for controls that looked functional but weren't; each made REAL or removed (app tsc 0, verified live on :8081, no console errors, re-exported). (1) **Bedtime reminder → REAL** local notification via newly-installed **expo-notifications** (`src/lib/reminders.ts` setBedtimeReminder → daily DAILY-trigger at 21:30, asks OS permission, cancels on off; returns false→toggle stays OFF if not actually scheduled). Native-only → the row is HIDDEN on web (remindersSupported guard) since local notifs don't work on web. Added `"expo-notifications"` to app.json plugins. (2) **Player "Download for offline" → REMOVED** (audio is bundled = already offline; nothing to download; real download lands when content is CDN-delivered). (3) **Community "Expert sessions / Notify me" → REMOVED** (the speculative shell I'd just added — no real sessions/notify behind it; wins wall stays, it's real). (4) **Family "Add a caregiver — Soon" → REMOVED** (placeholder). (5) **Account "Sleep goal" → REMOVED** (stored a value but sleep tracking was CUT → drove nothing). (6) **"Watch & learn / Short clips" → relabelled "Learn / Short reads"** (they're text articles, not video — no implying video that isn't there; real video is Glowco's content job, §6 "don't over-invest"). (7) **Kids "calm nights" stars → REAL** — were read from cc.calmNights (default 4) but NOTHING ever wrote it (fake "4 of 7" forever). New `src/lib/calmNights.ts` (getCalmNights default 0 / markCalmNightToday — increments once per calendar day, capped at goal 7, never decreases); KidsHome reads the real count (verified "0 of 7" on fresh state); Player calls markCalmNightToday() when a kid actually plays a session. (9) **First-run owner-match step (§6/§10)** — new `/claim-device` (`src/features/onboarding/ClaimDevice.tsx`): after SIGNUP, auto-runs `api.ownershipMatch` on the account email → verified-owner badge+warranty, else "Register my Glow Orb" (→register-device, real) or "Skip — I don't have one yet" (→home). Returning sign-ins skip straight to home. Registered route in _layout; verified live. (The "find my order with a DIFFERENT email" sub-path is deferred — needs Shopify + email verification keys; would reintroduce the ownership-oracle risk otherwise.) (8) **Real per-program progress** added `src/lib/programs.ts` (getProgramDone/markProgramStepDone, cc.programProgress) — ProgramDetail shows "X of N nights" + checkmarks (useFocusEffect refresh), "Begin/Continue · night N" resumes at first incomplete step, fixed the old "Begin program"→/player-with-no-id bug; Player marks the step done on play via ?program=&day= params. Non-failable (only fills in). tsc 0, re-exported. KNOWN keys-deferred placeholders LEFT (per user "leave keys last", honest in prod): IAP subscribe dev-grants premium in __DEV__/dev-fallback (prod requires real receipt validation, gated); Shopify ownership-match dev stub recognises @theglowcompany.co (prod hits real Shopify); GlowOrb is a stylised device stand-in (real pre-rendered 360 render is Glowco's asset). Servers up: :3000 / :8081 / :4000.

**FEATURE BUILD-OUT to §6/§7 IA 2026-06-23 ("continue building all the features", inline — agents still spend-limited):** Closed the remaining plan-IA gaps (app tsc 0, server build 0, verified live on :8081, no console errors, web re-exported): (1) **Library now organised by the §7 MOMENTS** (was generic rails) — SoundsLibrary ADULT_RAILS = Quick calm / Wind down & sleep / Music & sounds, then Watch & learn, then Programs (Programs moved last per plan order); all 8 tracks mapped across moments. (2) **Listen = Music + Sound machine** (§7) — added a "Music / Lyric-free, to drift to" section (MUSIC_IDS=['spa'], CMS-extensible) above the mixer, routes to Player w/ premium gate (kids never see paywall). (3) **Community "Expert sessions"** (§6 "expert-led sessions hosted by Glowco") — honest section (NO fabricated listings, consistent w/ the earlier wins-wall honesty): "Hosted by The Glow Company" card + persisted "Notify me" opt-in (cc.expertNotify); real sessions are Glowco's to publish. (4) **Anonymity toggle** (§6 "anonymous toggle, on by default") — added to Account preferences (cc.anonymous default true). Content registry still thin (8 tracks/seed) — moments organise what exists + are CMS-extensible; real library is Glowco's content pipeline. NOT-YET-BUILT (noted): Watch&learn is still text articles not video (plan says "don't over-invest"); per-program gentle progress tracking.

**NO-KEY COMPLETENESS BATCH 2026-06-23 (the four I could finish without keys; tsc 0, jest 7/7, web export 0, live-smoked, no console errors):** (1) **Profiles → backend sync**: ProfileProvider now uses useAuth token + a token-keyed effect that adopts the server household (`api.profiles`) as source of truth (offline-safe, sanitized), and addProfile POSTs `api.createProfile` (best-effort, next sync reconciles ids). (2) **Test harness**: installed jest-expo + jest; `jest.config.js` + `jest.setup.js` (AsyncStorage mock) + `npm test`; real tests `src/lib/__tests__/{programs,calmNights}.test.ts` (7 passing — progress idempotency/sort/isolation, calm-nights 0-default/once-per-day/goal-cap); tsconfig excludes `**/__tests__/**` from app tsc (jest runs them). (3) **Watch & learn → real video**: installed expo-video; `src/features/learn/WatchClip.tsx` (useVideoPlayer + VideoView, nativeControls) + `/watch` route; `Article.videoUrl` added (PLACEHOLDER sample on 'first-20'); Library + Learn route video items to /watch ("Watch · …") else reads; verified a real <video> renders on web. (4) **Error monitoring**: installed @sentry/react-native, platform-isolated `monitoring.ts` (web no-op) + `monitoring.native.ts` (Sentry.init gated on EXPO_PUBLIC_SENTRY_DSN — no DSN = no-op, never fake-reports); initMonitoring() in _layout. `.env.example` gained EXPO_PUBLIC_SENTRY_DSN. All four native modules (rn-iap, expo-video, sentry, apple/google auth) kept OUT of the web bundle via platform files / web support. STILL no real device build / real content / hosted legal.

**DESIGN-TOOLS PASS 2026-06-24 (user gave 21st Magic MCP + ui-ux-pro-max):** Added **21st Magic MCP** to user config (`claude mcp add magic -s user -e API_KEY=… -- npx -y @21st-dev/magic@latest`; connected ✔, loads NEXT session) — but it generates WEB (React-DOM/Tailwind) components, NOT usable in this RN app; value = visual reference only. ⚠️ the 21st API key was pasted in plaintext in chat → flagged to user to ROTATE; not committed/written anywhere. Used **ui-ux-pro-max** (`~/.claude/skills/ui-ux-pro-max-skill/src/ui-ux-pro-max/scripts/search.py "<q>" --domain ux|style|… --stack react-native [--design-system -p NAME]`) for design intel. DID NOT apply its color/type/pattern recs — they conflict with the LOCKED Glow brand (it suggested indigo/violet + Lora/Raleway + an app-store landing; plan §16 mandates teal/mint + Montserrat/Poppins). DID apply its applicable rules: (1) **loading states** — CommunityScreen now shows an ActivityIndicator while fetching (was flashing the empty "no wins" state); (2) **onboarding Back button** (had Skip+Next, now Skip+Next+Back per "provide Skip AND Back"). tsc 0, eslint 0 err, web export 0.

**STRESS TEST + BACKEND TESTS 2026-06-24 ("stress test... stop half-assing; deploy only at end"):** Ran autocannon load tests → **found a real prod bug**: the global rate limit (120/min) was so low it 429'd legit users (/content stress = 120 2xx then 313k 429s). FIXED: ThrottlerModule → 300 req/10s DoS backstop + `@SkipThrottle()` on /health. Re-stressed clean: /health (unthrottled, DB-ping/req) **33k req/s, 1.2ms avg, p99 4ms, 0 errors**; /events writes **300 landed under load, no crash**; RSS steady ~325MB (no leak); /content serves 200/200 rapid. Then stood up **backend test suite** (jest@29 + ts-jest@29; NOT @nestjs/testing — it needs Nest 11, we're on 10): `users.service.spec.ts` 6 tests incl. the EXPIRED-premium security regression guard; `npm test` in server + wired into CI server job. app jest 7 + server jest 6 = 13 tests green; app tsc 0, server build 0 (specs excluded via tsconfig.build.json). NOT deployed (per instruction — deploy at the end).

**PROD-CONCERNS PASS 2026-06-24 (user shared the "real tech SaaS" checklist images; "don't fake/bs"):** Added the genuinely-relevant no-keys infra: (1) **gzip compression** (`compression` middleware in main.ts) — throughput; verified Content-Encoding: gzip. (2) **catalog caching** — 60s in-memory TTL cache in ContentService.getCatalog (cuts a DB hit on every app-open), invalidated on CMS upsert; verified. (3) **/health is now a real readiness probe** — pings DB (SELECT 1) + uptimeSec via InjectDataSource; verified {ok,db:up,uptimeSec}. Server build 0. Honest scorecard given to user: HAVE (auth/permissions, rate-limit, security, CI, REST API, Postgres/embedded-SQLite, encryption-at-transit+secure-store, error boundary, Docker, caching, compression, health); KEYS-GATED (CDN/object-storage for media, Sentry DSN, push APNs/FCM, IAP, social, Shopify, managed Postgres+host); NOT-NEEDED-for-this-app & deliberately skipped (Kubernetes, Kafka/RabbitMQ/SQS, DynamoDB, Tensorflow, serverless/Lambda, websockets, RPC, long/short polling, sharding/partitioning, FTP). git: repo exists but **0 commits** (standing "DO NOT COMMIT" instruction — flagged to user, awaiting go-ahead). ("address all the issues that come with vibe coding"):** (1) **ESLint set up + run for the first time** (eslint-config-expo flat, React-Compiler rules). Fixed ALL 28 errors: removed unused imports/vars (BearMascot View, LibraryCard isNight, library.ts covers, RegisterDevice View, ClaimDevice View+c+useTheme), merged duplicate expo-router import (Caregivers), fixed import/first (_layout initMonitoring placement), file-level allow of RN require() for static assets (audio.ts), and **fixed ref-mutation-during-render** in ProfileProvider (profilesRef/tokenRef now written in a useEffect — React-Compiler safe). Tuned `eslint.config.js`: `react-hooks/immutability` OFF (Reanimated mutates sharedValue.value by design — was 21 false-positive errors), set-state-in-effect + exhaustive-deps → warn. Now **0 errors, 8 intentional warnings**; `npx eslint src` exit 0. (2) **Root ErrorBoundary** exported from `app/_layout.tsx` (expo-router) — render crashes show a calm brand-coloured "Let's take a breath / Try again" screen (plain RN primitives so it survives theme/font failure) + captureError to Sentry. No more white-screen. (3) **CI** `.github/workflows/ci.yml` — app job (npm ci → tsc → eslint → jest) + server job (build). (4) Pinned `@types/jest@29.5.14` (was 30, drift vs jest 29). Confirmed: NO console.log/TODO/debugger in src; only 2 documented `as never` (IAP OpenIAP generics); npm-audit vulns are dev-tooling-only (jest/eslint transitive), not shipped, not force-fixed. Verified green: app tsc 0, eslint 0 err, jest 7/7, server build 0, web export 0, app mounts, no console errors. Remaining honestly: test coverage still thin (2 suites/7 tests); the 8 lint warnings are intentional.

**SHARED CAREGIVERS (real household sharing) 2026-06-24 (user: "shared caregivers yes, offline downloads no"; monetization stays PDF subscription):** Built the v1/v2 "shared caregivers / one subscription covers everyone" feature for real (no fake "Soon"). Backend: `CaregiverLink` (unique caregiverOwnerId) + `CaregiverInvite` (unique code, 7-day expiry, single-use) entities; **@Global `HouseholdService.resolveOwnerId`** (caregiver→primary's id); `CaregiversModule` (`GET /caregivers`, `POST /caregivers/invite`→crypto code, `POST /caregivers/redeem`, `DELETE /caregivers/:id`; imports AuthModule for the JWT guard). Household resolution applied so a linked caregiver SHARES: entitlement (UsersService.getEffectiveEntitlement → premium inherited), devices (DevicesService.listForOwner + claim ownership), profiles (ProfilesService list/create/update/remove). PROVEN via curl: a fresh partner went free→**calm_plan/isPremium:true** after redeeming Sarah's code, and then saw Sarah's Glow Orb + household profiles; Sarah's list shows the partner. App: `api.{caregivers,inviteCaregiver,redeemCaregiver,removeCaregiver}` + `/caregivers` screen (invite→shows code, caregiver list w/ Remove, "Join a household" by code, "you're part of X's household" when a member) + a real Caregivers entry on Family (replaced the removed fake card). tsc 0, server build 0, web export 0, screen renders, no console errors. OFFLINE DOWNLOADS deliberately NOT built (user said no). Servers: :3000 static, :4000 API (:8081 dev exits on web export).

**"DO EVERYTHING + PLACEHOLDER KEYS" BUILD-OUT 2026-06-23 (4 waves, inline; app tsc 0, server build 0, web export 0, live-smoked, no console errors):** (1) **Backend production-ready**: installed `pg`; app.module branches Postgres (DATABASE_URL set) vs SQLite (dev); `synchronize` off in prod except one-time `DB_SYNC=true` bootstrap; added `Dockerfile` + `.dockerignore` + `fly.toml` + `DEPLOY.md`; prod boot guard now also requires DATABASE_URL; config gained databaseSsl/dbSync. (2) **Analytics (§15 funnel), first-party**: new `AnalyticsEvent` entity + `EventsModule` (`POST /events`, public/anonymous, validated) — PROVEN stores rows; client `src/lib/analytics.ts` (anon id via expo-crypto, fire-and-forget) instruments app_open / sign_up / sign_in / session_start / paywall_view / subscribe_success. (3) **IAP purchase flow**: installed **react-native-iap v15** (StoreKit2/OpenIAP); platform-isolated `src/lib/iap.ts` (web no-op) + `iap.native.ts` (fetchProducts/requestPurchase/listeners/finishTransaction → server-validated via api.billingValidate; PLACEHOLDER product ids calmcarry.premium.monthly|annual); CalmPlan uses native purchase on device, dev/web falls back to the existing path; added react-native-iap config plugin. WEB EXPORT stays green (web never imports rn-iap). (4) **Apple/Google sign-in (§6 "no passwords")**: installed expo-apple-authentication + expo-auth-session; `api.social` + `AuthProvider.socialSignIn` → backend `/auth/social`; SignIn shows Apple button (real iOS, no key) + Google button (only when EXPO_PUBLIC_GOOGLE_* set — hidden on placeholder so no dead button); email/password kept as fallback. App `.env.example` documents EXPO_PUBLIC_API_BASE + Google client ids. Still keys-deferred to actually transact: real store products, Google client ids, Apple IAP shared secret, Shopify, CDN, push — all placeheld + degrade gracefully. NOT yet: real device build (eas.json present, never run), expo-video watch&learn, profiles backend sync, tests. Servers: :3000 static, :8081 dev, :4000 API.

**CALM MOTION + FEELING CHECK-IN 2026-06-23 (user: "motion when clicking, calming/smooth navigating + more questions on how they're feeling — anxious insomniac etc"):** (1) **Calmer navigation** — added `dur.screen=420` token + set the default expo-router Stack transition to `animation:'fade', animationDuration:dur.screen` (slow calm cross-fade vs RN default). Cards already had scale-0.97+haptic press; added the same to the new check-in rows (`TapRow` w/ Reanimated). (2) **Feeling step (the "more questions" ask) — REFRAMED to SAFE WORDS.** User said "anxious insomniac" but that VIOLATES the plan's #1 golden rule (§3/§14 no "anxiety/insomnia/ADHD" anywhere — Google Play requires it; app-store reject risk). Flagged to user + built with safe words instead: `Feeling` type + `FEELING_MAP` in ProfileProvider — 6 states **My mind's racing / I can't switch off / Wired but tired / I'm wound up / It's been a heavy day / I just want quiet**, each maps forward-looking → intent + recommended track + a warm line (NO symptom tracking/history; persisted `cc.feeling`; `recommendedTrackId` prefers the feeling's track). MoodSurvey is now a 2-step check-in: "How are you arriving tonight?" (feeling) → "What would feel good right now?" (intent, the feeling's match highlighted + warm line e.g. "Let's slow the spin."). Picking a feeling also seeds the matching intent so stopping after 1 tap still tailors. VERIFIED live on :8081: step1 6 feelings + heading, ZERO banned words; tap→step2 with warm line + 4 intents; no console errors. app tsc 0, web re-exported. User can override to literal clinical labels but it's flagged as a rejection risk. Servers: static :3000, dev :8081, API :4000 all up.

**EXPO v56 + BUILD-PLAN ALIGNMENT PASS 2026-06-23 (user pushback: "are u aligned with the docs i gave u, bcz ur not" — and they were right):** I had NEVER read the AGENTS.md-mandated Expo v56 versioned docs nor re-grounded in the build plan PDF. Fixed: read `~/Downloads/CalmCarry_App_Build_Plan.pdf` (9pp — extract via `pip3 install pypdf`; poppler/Quartz absent on this Mac) + WebFetched the real v56 docs for secure-store/local-authentication/crypto/audio. **Installed `expo-secure-store` + `expo-local-authentication` + `expo-crypto` (npx expo install, SDK-pinned).** Fixes (app tsc 0, nest build 0, web export 0, live-verified): (1) **Auth token → Keychain/Keystore** via new `src/lib/secureStore.ts` (SecureStore native, AsyncStorage web fallback since SecureStore has NO web support; one-time migration from old AsyncStorage key); AuthProvider read/write/clear token through it. (2) **Parent PIN rewritten** to expo-crypto **SHA-256 salted** (was hand-rolled JS hash) + stored in secure store, lockout kept. (3) **Biometric parent gate** (plan §13/§9 "PIN or Face ID") via expo-local-authentication — `biometricAvailable()`/`authenticateBiometric()`, ParentGate auto-prompts Face ID/Touch ID on the enter screen + a button, PIN stays fallback. (4) **Background audio (plan §12 all-night, screen-off)** — was BROKEN: `setAudioModeAsync({playsInSilentMode})` only + empty `app.json` infoPlist. Fixed per v56 audio doc: added `shouldPlayInBackground:true` to all 3 setAudioModeAsync calls + `app.json` plugin `["expo-audio",{enableBackgroundPlayback:true}]`. (5) **Player loop bug** — looped EVERYTHING incl. guided sessions/sleep tales; now `player.loop = track.category==='soundscape'` only (§6 "gentle end"). (6) **COPPA verifiable parental consent (§13, deadline Apr-2026 PAST)** — was MISSING: new `src/lib/consent.ts` (hasCoppaConsent/recordCoppaConsent) + Family.tsx gates kid-profile creation behind an informed consent panel (records timestamp+version). NOTE: FTC-approved VPC *method* (payment/ID) = Glowco launch decision. (7) **Account & data deletion (Apple 5.1.1(v) + COPPA/GDPR)** — was MISSING: `UsersService.deleteAccount` (DataSource transaction, cascades warranty_claims→saved_mixes→community_posts/session_logs/push_tokens/devices/profiles/entitlements→owner) + `DELETE /me` (JwtAuthGuard) + `api.deleteAccount` + AccountScreen two-step "Delete account". PROVEN: register+data → DELETE → all rows 0, token→404. **Medical-word sweep CLEAN** (only the required "not intended to diagnose/treat/cure" disclaimer + community moderation regex). **KidsGuard re-confirmed live** (adult /you in kids mode → redirected to kid home). KNOWN-DOC DIVERGENCES still open (flagged, not yet changed): plan §6 says "one-tap sign-in, NO passwords" but app has email+password (social needs keys); button radius 16px vs plan's 8px; non-looping audio is a CONTENT requirement (assets must be long/seamless — Mason's pipeline). **CONSTRAINT THIS SESSION: multi-agent Workflow/Agent fan-out hit the monthly spend limit** (the 9-lens total-audit workflow died mid-run, 0 findings) → did this pass inline in the main loop. server `npm audit`: 2 high/8 moderate (transitive; not force-fixed). STILL NOT COMMITTED.
  - **INLINE CONTINUE (more lenses, no agents):** (8) Removed vestigial **`settledRating`** end-to-end (SessionLog entity column + CreateLogDto + logs.service/controller + sessions.ts + api.ts) — the app never sent it, but a persisted "settle rating" = mood history, which the plan forbids (§3 "never feel like a patient", §13 data minimization). `logSession` now records only contentId/deviceId/time (legit §15 activation analytics). DB wiped+reseeded (schema change); verified session_logs cols = id,ownerId,deviceId,contentId,startedAt. (9) **Shopify webhook now throws 401 on a forged HMAC** (was returning 200 {ok:false}) — prod-path only (dev w/o secret still accepts, by design). Breadth lenses run inline came back CLEAN: all timers/intervals have cleanup (Player cue/breath, wind-down idle, ListenScreen sleep-timer, ParentGate lock); backend check-in is genuinely ephemeral (no entity, recommend() stores nothing); ListenScreen sound-mixer tiles + volume pegs already have role/state/label a11y. (10) **Button radius → 8px** (plan §16; was 16px) in PrimaryButton — verified live (computed border-radius 8px). (11) **profiles GET race fixed**: partial unique index `uq_profile_primary ON profiles(ownerId) WHERE isPrimary=1` + list() catches the conflict and re-reads — PROVEN: 4 concurrent first-GETs → exactly 1 profile. Remaining open = product-DECISIONS only (no-password one-tap sign-in needs Apple/Google keys; non-looping audio = content/asset job for Glowco). Static :3000 + API :4000 left running; :8081 dev server exits during web export (HMR only, restart via preview_start if needed).

**HARSH SECURITY AUDIT + FIXES 2026-06-23 (10-lens workflow → adversarial verify → 53 confirmed of 67; fixed; verified: nest build 0, app tsc 0, live curl + DB proofs, no console errors):** Added `@nestjs/throttler` + `helmet` to the server. **CRITICALS:** (1) **Receipt replay across accounts** — `users.service.grantSubscription` dedup was scoped to (ownerId, transactionRef) with no global uniqueness, so one valid Apple receipt could upgrade unlimited accounts. Fixed: look up by transactionRef ALONE → `ConflictException` if bound to another owner; added a **partial unique index** `uq_entitlement_txn ON entitlements(transactionRef) WHERE transactionRef IS NOT NULL`. PROVEN: 2nd owner insert of same ref → `SQLITE_CONSTRAINT_UNIQUE`. (2) **Social-auth signature NEVER verified even in prod** — the `verified` flag was just "client-id env present", so a forged `{alg:none, email:victim}` JWT = account takeover. Fixed: `SIGNATURE_VERIFICATION_IMPLEMENTED=false` constant → prod **fails closed** (`ServiceUnavailableException`); dev decode-only + warn; added `emailVerified` and block social-merge into an existing account on unverified email. **HIGHS:** expired premium still unlocked (status-only check) → added expiry-aware `UsersService.isPremium()`/`isPremiumEntitlement()` used by the content gate + billing/status (PROVEN: lapsed calm_plan → isPremium:false + signed-url 403); default `JWT_SECRET`/`CMS_ADMIN_KEY`/missing-CDN now block prod boot (rewrote `main.ts` guard — dropped the dead DATABASE_URL check the app never used, added CDN + CORS_ORIGINS reqs); no rate-limit → global ThrottlerGuard 120/min + 10/min on auth routes (PROVEN: 11th login → 429); device serial now dedup/ownership-checked (`ConflictException`); demo known-creds user seed gated `!isProd`; parent-gate PIN was reversible obfuscation + no lockout → now a **salted non-reversible hash** (pure-JS, no native dep) + **5-try lockout/60s** (`parentPinLockSeconds` + UI countdown in ParentGate.tsx; PROVEN deterministic+non-reversible+lockout). **MEDIUMS:** mass-assignment → `forbidNonWhitelisted:true` (PROVEN: injected `tier` field → 400); CMS key → `crypto.timingSafeEqual` + throttle; Apple receipt → premium-SKU allowlist (`config.premiumProductIds` / `PREMIUM_PRODUCT_IDS` env); `synchronize:!isProd`; CDN refuses unsigned in prod; ownership/match uses ONLY the authed email (was client-supplied — oracle/badge-spoof); helmet headers (CSP/HSTS/X-Frame/nosniff — PROVEN present); sqlite file `chmod 600` on boot; body-size cap 64kb. **LOWS:** JWT `algorithms:['HS256']` pinned; login does a dummy bcrypt on unknown email (timing-equalized enumeration); unbounded finds capped `take:200`; crypto-random warranty refs (was Math.random); KidsGuard now waits on **profile hydration** before first paint (native — no adult-frame flash before a kid profile restores); ageBand `@IsIn` enum; length caps on all DTOs (password 128 → bcrypt-DoS cap); root `.gitignore` now ignores `.env*`/`**/data/*.sqlite`. **ACCEPTED/DEFERRED (documented, not bugs):** premium audio is currently BUNDLED royalty-free placeholder played directly — a forged client `isPremium` can play those bundled demo tracks offline; the REAL fix is shipping premium content via the (now-hardened, expiry-aware, server-gated) signed-URL CDN endpoint, not the bundle — Mason's content-pipeline task. JWT in AsyncStorage→localStorage on web (XSS-readable) is standard for this stack; http API base is the localhost DEV default (set EXPO_PUBLIC_API_BASE=https in prod). Social-auth real JWKS verify + Google Play lookup still to wire (prod fails closed until then). Servers running: API :4000, static :3000, dev :8081. STILL NOT COMMITTED.

**HARSH DEV+CONSUMER AUDIT + FIXES 2026-06-23 (verified: app tsc 0 errors, nest build 0, fresh DB reseed, live browser smoke on :8081, no console errors):** Found+fixed real trust/security holes — NOT cosmetic. **Backend security:** (a) IAP `receipt-validation.service` devGrant now GATED on `devFallback` (apple()/google() throw `ServiceUnavailableException` in prod w/o keys) — was free-premium-for-anyone in prod. (b) `social-auth.service` throws `UnauthorizedException` in prod when signature unverified (was account-takeover-by-email-claim); added `social-login.dto` (`@IsIn(['apple','google'])` + idToken `@MaxLength(8192)`) — verified bad provider→400. (c) `main.ts` prod boot guard: exits if JWT_SECRET/CMS_ADMIN_KEY are defaults or DATABASE_URL missing; CORS allowlist in prod (`corsOrigins` in config). (d) **server-side paywall** — `content.service.signedUrl(id, ownerId)` now checks `item.locked`→requires premium via UsersService else `ForbiddenException` (was: anyone could fetch locked audio URLs). **VERIFIED: free user→403, premium→200, free track→200.** (e) `users.service.grantSubscription` upserts by (ownerId, transactionRef) — no dupe entitlements. **App honesty (consumer):** removed prefilled demo creds + fake "Continue with Glow Company" button from SignIn (real signup/signin toggle + error states + `register`); AuthProvider offline path grants FREE not premium, rethrows 401/403 (was: any creds logged in w/ premium). **Player** redirects locked tracks→/unlock + gates Download behind premium. **ParentGate**: creating a PIN no longer also exits kids mode. **KidsGuard** redirects synchronously during render (closed post-mount race). **Verified-owner chip** only shows on real `api.ownershipMatch`. Device screens (Register/Replacement/Authenticity) now make REAL API calls + only show success on success — **AuthenticityCheck** was always-"Authentic"+hardcoded serial→now verifies against the account's registered device (honest "can't confirm"+register CTA when none). **Family** loads real `api.devices` + empty state (was hardcoded "Sarah's/Leo's Orb" shown to everyone). **wind-down** plays the recommended track from `?id=` param (was hardcoded "Slow tide" for all). **Community fabrication KILLED both ends:** frontend dropped the 4 fake testimonials + "248 winding down" → real empty state; backend `presence()` returns real approved-post count (was `180+count*7`) + **removed the community seed entirely** (seeding fake testimonials = dishonest social proof / app-store risk) — wiped+reseeded DB, wall now genuinely empty. ListenScreen clears sleep-timer on unmount; ProfileProvider sanitizes corrupt persisted profiles. STILL NOT COMMITTED. Servers running: API :4000 (pid fresh, dev-fallback), static :3000, dev :8081.
